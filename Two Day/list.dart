import 'dart:ffi';
import 'dart:io';

void main() {
  List<int> number = [10, 5, 7, 3, 2, 1];
  // sort untuk mengurutkan nilai array dari terkecil
  // shuffle() untuk membalikkan nilai array
  // number.sort();
  print(number.sublist(1, 5));
  // print(number);
}
